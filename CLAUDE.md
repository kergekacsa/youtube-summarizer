# CLAUDE.md

## Agent skills

### Issue tracker

Issues live in the Nimbalyst local tracker (`.nimbalyst/trackers/`), accessed via the `mcp__nimbalyst-mcp__tracker_*` MCP tools. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical role names, applied as `tags` on tracker items. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` and `docs/adr/` at the repo root, created lazily by `/grill-with-docs`. See `docs/agents/domain.md`.
